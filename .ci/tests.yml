platform: linux

image_resource:
  type: registry-image
  source:
    repository: golang
    tag: 1.25

inputs:
- git_code

run:
  path: /usr/bin/env
  dir: git_code
  args:
  - /bin/bash
  - -ec
  - |-
    go test ./...

